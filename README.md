# Part 1
## Part 1a
- Website uses **EJS templating** and **partials** 
- Created **header and footer partials** in `header.ejs` and `footer.ejs`.
- Each page (`index.ejs`, `about.ejs`, `projects.ejs`, `contact.ejs`) has partials for `header` and `footer`

## Part 1b
- The `index.js` file handles the routes for ALL 4 pages
- They are accessible using the url `http://localhost:3000` with `/`, `/about`, `/projects`, and `/contact`


# Part 2
## Part 2a
- Generated site structure with **Express Generator** using `--views=ejs`

## Part 2b
- Moved all necessary files into the **Express Folder** titled **Assignment-2**
- Deleted any files deemed unecessary or were already created by the **Express Folder**

## Part 2c
- Inserted **Bootstrap CSS** and **Font Awesome** in the `header.ejs` file